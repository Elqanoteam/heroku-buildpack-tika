#!/bin/bash

set -e

BUILDPACK_DIR=`cd $(dirname $0); cd ..; pwd`
BUILD_DIR=$1
CACHE_DIR=$2
VER=1.20

cd $CACHE_DIR
curl -O https://www.apache.org/dyn/closer.cgi/tika/tika-app-${VER}.jar

cp tika-app-${VER}.jar $BUILD_DIR/vendor/jar/tika-app-${VER}.jar
